@model IEnumerable<WebConsultas.Models.Perfil>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idPerfil }) |
            @Html.ActionLink("Details", "Details", new { id=item.idPerfil }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idPerfil })
        </td>
    </tr>
}

</table>
