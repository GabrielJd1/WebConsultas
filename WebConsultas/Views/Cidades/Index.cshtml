@model IEnumerable<WebConsultas.Models.Cidade>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Estado.descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Estado.descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idCidade }) |
            @Html.ActionLink("Details", "Details", new { id=item.idCidade }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idCidade })
        </td>
    </tr>
}

</table>
